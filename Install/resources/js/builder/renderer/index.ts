/**
 * Builder renderer — canvas renderer.
 * Renders component tree via registry: lookup → merge defaults → render.
 * No direct component imports; always uses registry.
 */

export { CanvasRenderer } from './CanvasRenderer';
export type { CanvasRendererProps } from './CanvasRenderer';
